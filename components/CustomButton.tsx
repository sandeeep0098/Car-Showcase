'use client'
import Image from "next/image"

const CustomButton = () => {
  return (
    <button
    disabled={false}
    type={"button"}
    className={"custom-btn"}
    onClick={()=>{}}

    >
i am button
    </button>
  )
}

export default CustomButton